source "https://rubygems.org"

# Matches the GitHub Pages build environment (includes jekyll, jekyll-feed,
# jekyll-sitemap). For local preview: `bundle install && bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
